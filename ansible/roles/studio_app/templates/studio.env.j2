# Managed by Ansible (ansible/roles/studio_app/templates/studio.env.j2) - edit group_vars/vault.yml
# and re-run the playbook instead of editing this file directly, it'll just get overwritten.

PORT={{ studio_port }}
DB_PATH={{ studio_home }}/data/studio.db
MEDIA_STORAGE_DIR={{ studio_home }}/data/media-storage

AUTH_SECRET={{ studio_auth_secret }}
ALLOW_DEV_LOGIN={{ studio_allow_dev_login }}
APP_URL={{ studio_app_url }}

BOOTSTRAP_ADMIN_NAME={{ studio_bootstrap_admin_name }}
BOOTSTRAP_ADMIN_EMAIL={{ studio_bootstrap_admin_email }}

SMTP_HOST={{ studio_smtp_host }}
SMTP_PORT={{ studio_smtp_port }}
SMTP_USER={{ studio_smtp_user }}
SMTP_PASS={{ studio_smtp_pass }}
SMTP_FROM={{ studio_smtp_from }}
