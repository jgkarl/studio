# Copy to .env for local (non-Docker) runs, or edit directly on the VPS — see docs/deploy.md.

PORT=3000

# Filesystem path to the SQLite database file. The parent directory is created automatically if
# it doesn't exist. Back this up along with MEDIA_STORAGE_DIR below — together they're the whole
# app's state.
DB_PATH=./data/studio.db

# Signs session cookies and verification tokens — generate a real one for anything beyond a
# local throwaway: `openssl rand -hex 32`
AUTH_SECRET=dev-only-insecure-secret-change-me

# If all three are set, a User with this name/email/password is created (role admin, provider
# "email", already email-verified — signs in through the normal /login form immediately) the
# first time the app boots against a database that doesn't already have a User with this email.
# This is the only way to get a first account on a brand new database without a database console
# — there's no dev-login picker, so these three are how every deploy (dev or production) actually
# signs in for the first time. Safe to leave set permanently (idempotent — a no-op once the
# account exists).
BOOTSTRAP_ADMIN_NAME=
BOOTSTRAP_ADMIN_EMAIL=
BOOTSTRAP_ADMIN_PASSWORD=

# Set to true to also seed fictional demo content on first boot: a second "conservator" example
# user (see internal/seed/demo.go for its password), two clients, two assets with condition
# history, a project, a treatment, a report, and a few example media library images — dev/local
# convenience only, never set this for a production deploy.
SEED_EXAMPLE_DATA=true

# Absolute or relative path where uploaded media (originals + generated variants) is stored.
# Kept under the same data/ directory as DB_PATH on purpose — one directory to back up, and it
# matches deploy/studio.service's ReadWritePaths= (systemd hardening only grants write access to
# data/, not the whole checkout).
MEDIA_STORAGE_DIR=./data/media-storage

# Absolute origin used to build links in emails (verification, password reset). Falls back to the
# incoming request's Host header if unset.
APP_URL=

# SMTP — leave blank locally; unset SMTP_HOST logs email bodies to the server console instead of
# sending them.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=studio@localhost

# Logging (internal/logging, internal/httplog) — every request gets one access-log line
# (method/path/status/duration/client IP), and every log line carries a request_id tying it to
# whatever request triggered it, plus category (module) and event (short slug) fields. LOG_LEVEL:
# debug/info/warn/error, default info — set debug while actively troubleshooting a live issue.
# LOG_FORMAT: text (human-readable, easy to read directly in `journalctl -u studio -f`) or json
# (machine-parseable, pipe through `jq`) — only affects the console stream. LOG_DIR: also writes a
# JSON copy to LOG_DIR/studio.log regardless of LOG_FORMAT (blank disables this, console-only);
# on the VPS this is rotated at 10MB by /etc/logrotate.d/studio (see
# ansible/roles/studio_app/templates/studio-logrotate.conf.j2) — locally it just grows unrotated.
LOG_LEVEL=info
LOG_FORMAT=text
LOG_DIR=./data/log
