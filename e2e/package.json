{
  "name": "stuudio-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright end-to-end suite for the Stuudio app, driven against a running server (Docker or `make run`) via BASE_URL.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
