# Optional local dev convenience: `docker compose up --build` (or `podman-compose up --build`).
# Not the VPS deployment path — see docs/deploy.md for the Ansible-driven approach (installs
# libvips42 + the released binary directly on a plain Ubuntu 24.04 VPS, no container runtime
# there at all). This file is still useful for a quick local try without installing
# Go/libvips/templ yourself.
#
# SQLite is a single file — no separate database service needed at all anymore. Everything the
# app owns (the .db file + media-storage/) lives under one bind-mounted host directory so it's a
# plain directory you can back up or inspect directly, not an opaque named volume.
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: localhost/studio:latest
    ports:
      - "${PORT:-3000}:3000"
    environment:
      AUTH_SECRET: "${AUTH_SECRET:-dev-only-insecure-secret-change-me}"
      # No dev-login picker — these three are the only way in. Compose only forwards vars listed
      # here (unlike DB_PATH/MEDIA_STORAGE_DIR/PORT, which the Dockerfile already sets to match
      # the /data volume mount), so BOOTSTRAP_ADMIN_* has to be explicit or the container boots
      # with no usable login at all.
      BOOTSTRAP_ADMIN_NAME: "${BOOTSTRAP_ADMIN_NAME:-}"
      BOOTSTRAP_ADMIN_EMAIL: "${BOOTSTRAP_ADMIN_EMAIL:-}"
      BOOTSTRAP_ADMIN_PASSWORD: "${BOOTSTRAP_ADMIN_PASSWORD:-}"
      SEED_EXAMPLE_DATA: "${SEED_EXAMPLE_DATA:-true}"
    volumes:
      - ./data:/data
    restart: unless-stopped
